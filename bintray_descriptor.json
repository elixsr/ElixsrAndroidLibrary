{
  "package": {
    "name": "ElixsrAndroidCore",
    "repo": "elixsr-android-core",
    "subject": "elixsr"
  },

  "version": {
    "name": "1.0"
  },
  "files":
  [
    {"includePattern": ".*/build/outputs(.*)*/(.*\\.aar)", "excludePattern": ".*/do-not-deploy/.*", "uploadPattern": "$1"},
    {"includePattern": ".*/build/docs/(.*)", "uploadPattern": "docs/$2"}
  ],
  "publish": true
}